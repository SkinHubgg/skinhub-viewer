{
	"name": "@skinhub/viewer-example",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"description": "A customer's app. Imports @skinhub/viewer through node_modules and nothing else from this repo.",
	"scripts": {
		"start": "bun run serve.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@skinhub/viewer": "file:..",
		"react": ">=18",
		"react-dom": ">=18"
	}
}
